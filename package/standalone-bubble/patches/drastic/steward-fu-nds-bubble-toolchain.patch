diff --git a/Makefile.base b/Makefile.base
index 0bfb270..23efc3d 100644
--- a/Makefile.base
+++ b/Makefile.base
@@ -1,6 +1,7 @@
 ifeq ($(NDS_GCC),1)
 export CROSS=arm-linux-gnueabihf-
-export PATH=/opt/mini/bin:$(shell echo $$PATH)
+export PATH=$(TOOLCHAIN_BIN):$(shell echo $$PATH)
+export NDS_INCLUDE_ROOT
 endif
 
 export CC=${CROSS}gcc
diff --git a/sdl2/configure.ac b/sdl2/configure.ac
index 7c6844a..4d121f7 100644
--- a/sdl2/configure.ac
+++ b/sdl2/configure.ac
@@ -2412,7 +2412,8 @@ CheckNDSVideo()
         EXTRA_CFLAGS="$EXTRA_CFLAGS -I../detour"
         EXTRA_CFLAGS="$EXTRA_CFLAGS -I../common"
         EXTRA_CFLAGS="$EXTRA_CFLAGS -I../inc/sdl2"
-        EXTRA_CFLAGS="$EXTRA_CFLAGS -I/opt/mini/arm-buildroot-linux-gnueabihf/sysroot/usr/include/SDL2"
+        EXTRA_CFLAGS="$EXTRA_CFLAGS -I$NDS_INCLUDE_ROOT/SDL2"
+        EXTRA_LDFLAGS="$EXTRA_LDFLAGS -L../drastic/lib"
 
         EXTRA_LDFLAGS="$EXTRA_LDFLAGS -lEGL"
         EXTRA_LDFLAGS="$EXTRA_LDFLAGS -lGLESv2"
@@ -2443,7 +2443,7 @@ CheckNDSVideo()
 
     if test x$MOD = xmiyoo_flip; then
         EXTRA_CFLAGS="$EXTRA_CFLAGS -DMIYOO_FLIP"
-        EXTRA_CFLAGS="$EXTRA_CFLAGS -I/opt/mini/arm-buildroot-linux-gnueabihf/sysroot/usr/include/libdrm"
+        EXTRA_CFLAGS="$EXTRA_CFLAGS -I$NDS_INCLUDE_ROOT/libdrm"
 
         EXTRA_LDFLAGS="$EXTRA_LDFLAGS -ldrm -lgbm"

diff --git a/runner/Makefile b/runner/Makefile
index 45779ba..5810d3a 100644
--- a/runner/Makefile
+++ b/runner/Makefile
@@ -7,9 +7,8 @@ ifeq ($(MOD),gkd_pixel2)
 endif
 
 ifeq ($(MOD),gkd_miniplus)
-export CROSS=aarch64-linux-
-export PATH=/opt/flip/bin:$(shell echo $$PATH)
+export CROSS=$(NDS_RUNNER_CROSS)
+export PATH=$(NDS_RUNNER_TOOLCHAIN_BIN):$(shell echo $$PATH)
 CFLAGS  += -DGKD_MINIPLUS
-CFLAGS  += -I/opt/flip/aarch64-flip-linux-gnu/sysroot/usr/include/SDL2
-LDFLAGS += -ljson-c
+CFLAGS  += -I$(NDS_RUNNER_INCLUDE_ROOT)/SDL2
 endif
@@ -44,12 +43,11 @@ TARGET   = runner
 CFLAGS  += -DRUNNER
 CFLAGS  += -O3
 CFLAGS  += -I.
 CFLAGS  += -I../common
 LDFLAGS += -lSDL2
-LDFLAGS += -lSDL2_image
 LDFLAGS += -lEGL
 LDFLAGS += -lGLESv2
-SOURCES  = runner.c ../common/common.c
+SOURCES  = runner.c
 
 .PHONY: all
 all:
diff --git a/runner/runner.c b/runner/runner.c
index 689d67f..9ddf5fb 100644
--- a/runner/runner.c
+++ b/runner/runner.c
@@ -10,5 +10,6 @@
 #include <unistd.h>
 #include <signal.h>
+#include <string.h>
 #include <sys/types.h>
 #include <sys/stat.h>
 #include <sys/socket.h>
@@ -17,6 +18,5 @@
 
 #define SDL_DISABLE_ARM_NEON_H
 #include <SDL.h>
-#include <SDL_image.h>
 #include <GLES2/gl2.h>
 #include <SDL2/SDL.h>
@@ -27,2 +27,22 @@
 static const char *DEBUG_LEVEL_STR[] = { "FATAL", "ERROR", "DEBUG", "TRACE" };
+int nds_debug_level = ERROR_LEVEL;
+
+static int runner_debug_level(void)
+{
+    const char *level = getenv("NDS_DEBUG_LEVEL");
+
+    if (!level || !*level || !strcmp(level, "ERROR")) {
+        return ERROR_LEVEL;
+    }
+    if (!strcmp(level, "FATAL")) {
+        return FATAL_LEVEL;
+    }
+    if (!strcmp(level, "DEBUG")) {
+        return DEBUG_LEVEL;
+    }
+    if (!strcmp(level, "TRACE")) {
+        return TRACE_LEVEL;
+    }
+    return ERROR_LEVEL;
+}
 
@@ -81,6 +101,28 @@ static int running = 0;
 static int frame_cnt = 0;
 static runner_t myrunner = { 0 };
 
+static int check_shader(GLuint shader, const char *name)
+{
+    GLint ok = GL_FALSE;
+    GLchar log[1024] = { 0 };
+
+    glGetShaderiv(shader, GL_COMPILE_STATUS, &ok);
+    glGetShaderInfoLog(shader, sizeof(log) - 1, NULL, log);
+    debug("gles shader=%s compiled=%d log=%s\n", name, ok, log);
+    return ok == GL_TRUE ? 0 : -1;
+}
+
+static int check_program(GLuint program)
+{
+    GLint ok = GL_FALSE;
+    GLchar log[1024] = { 0 };
+
+    glGetProgramiv(program, GL_LINK_STATUS, &ok);
+    glGetProgramInfoLog(program, sizeof(log) - 1, NULL, log);
+    debug("gles program linked=%d log=%s\n", ok, log);
+    return ok == GL_TRUE ? 0 : -1;
+}
+
 static int init_shm(void)
 {
     trace("call %s()\n", __func__);
@@ -118,20 +160,47 @@ static int init_gles(void)
     SDL_GL_SetAttribute(SDL_GL_CONTEXT_MINOR_VERSION, 0);
 
     myrunner.sdl2.win = SDL_CreateWindow("DraStic", 0, 0, R_LCD_W, R_LCD_H, SDL_WINDOW_OPENGL);
+    if (!myrunner.sdl2.win) {
+        error("failed to create sdl window: %s\n", SDL_GetError());
+        return -1;
+    }
     myrunner.gles.ctx = SDL_GL_CreateContext(myrunner.sdl2.win);
+    if (!myrunner.gles.ctx) {
+        error("failed to create gles context: %s\n", SDL_GetError());
+        return -1;
+    }
+    if (SDL_GL_MakeCurrent(myrunner.sdl2.win, myrunner.gles.ctx) != 0) {
+        error("failed to make gles context current: %s\n", SDL_GetError());
+        return -1;
+    }
+    debug("gles context sdl=%p current_sdl=%p current_egl=%p display=%p draw=%p read=%p\n",
+        myrunner.gles.ctx, SDL_GL_GetCurrentContext(), eglGetCurrentContext(),
+        eglGetCurrentDisplay(), eglGetCurrentSurface(EGL_DRAW),
+        eglGetCurrentSurface(EGL_READ));
+    debug("gles vendor=%s renderer=%s version=%s\n",
+        glGetString(GL_VENDOR), glGetString(GL_RENDERER), glGetString(GL_VERSION));
  
     myrunner.gles.vert.shader = glCreateShader(GL_VERTEX_SHADER);
     glShaderSource(myrunner.gles.vert.shader, 1, &vert_shader_code, NULL);
     glCompileShader(myrunner.gles.vert.shader);
+    if (check_shader(myrunner.gles.vert.shader, "vertex") < 0) {
+        return -1;
+    }
  
     myrunner.gles.frag.shader = glCreateShader(GL_FRAGMENT_SHADER);
     glShaderSource(myrunner.gles.frag.shader, 1, &frag_shader_code, NULL);
     glCompileShader(myrunner.gles.frag.shader);
+    if (check_shader(myrunner.gles.frag.shader, "fragment") < 0) {
+        return -1;
+    }
  
     myrunner.gles.object = glCreateProgram();
     glAttachShader(myrunner.gles.object, myrunner.gles.vert.shader);
     glAttachShader(myrunner.gles.object, myrunner.gles.frag.shader);
     glLinkProgram(myrunner.gles.object);
+    if (check_program(myrunner.gles.object) < 0) {
+        return -1;
+    }
     glUseProgram(myrunner.gles.object);
  
     myrunner.gles.vert.tex_pos = glGetAttribLocation(myrunner.gles.object, "vert_tex_pos");
@@ -143,6 +212,7 @@ static int init_gles(void)
 
     glUniform1f(myrunner.gles.frag.rotate, 0);
     glUniform1f(myrunner.gles.frag.aspect, (float)R_LCD_W / R_LCD_H);
+    glUniform1f(myrunner.gles.frag.alpha, 1.0f);
       
     glPixelStorei(GL_UNPACK_ALIGNMENT, 1);
     glGenTextures(TEXTURE_MAX, myrunner.gles.texture);
@@ -166,6 +236,10 @@ static int init_gles(void)
     glUniform1i(myrunner.gles.frag.tex_main, 0);
     glDrawElements(GL_TRIANGLES, 6, GL_UNSIGNED_SHORT, vert_indices);
 
+    debug("gles init error=0x%x attrib_pos=%d attrib_coord=%d uniform_alpha=%d\n",
+        glGetError(), myrunner.gles.vert.tex_pos, myrunner.gles.vert.tex_coord,
+        myrunner.gles.frag.alpha);
+
     myrunner.gles.bg.pixels = malloc(R_LCD_W * R_LCD_H * 4);
     if (!myrunner.gles.bg.pixels) {
         error("failed to allocate buffer for bg image\n");
@@ -301,13 +375,21 @@ static void* runner_handler(void *param)
                 (myrunner.shm.buf->layout == LAYOUT_MODE_N1)) &&
                 (myrunner.shm.buf->tex == TEXTURE_LCD0))
             {
-                glUniform1f(myrunner.gles.frag.alpha, 0.0);
+                glUniform1f(myrunner.gles.frag.alpha, 1.0f);
                 glDisable(GL_BLEND);
             }
             break;
         case SHM_CMD_FLIP:
             frame_cnt += 1;
             trace("recv SHM_CMD_FLIP\n");
+            if (frame_cnt <= 3) {
+                unsigned char pixel[4] = { 0 };
+                glReadPixels(R_LCD_W / 2, R_LCD_H / 2, 1, 1,
+                    GL_RGBA, GL_UNSIGNED_BYTE, pixel);
+                debug("gles frame=%d center=%u,%u,%u,%u error=0x%x\n",
+                    frame_cnt, pixel[0], pixel[1], pixel[2], pixel[3],
+                    glGetError());
+            }
             SDL_GL_SwapWindow(myrunner.sdl2.win);
 
             glActiveTexture(GL_TEXTURE0);
@@ -361,5 +443,6 @@ int main(int argc, char **argv)
 {
     pthread_t id = 0;
 
+    setvbuf(stdout, NULL, _IONBF, 0);
-    nds_debug_level = get_debug_level(0);
+    nds_debug_level = runner_debug_level();
     trace("log level \"%s\"\n", DEBUG_LEVEL_STR[nds_debug_level]);
@@ -365,16 +448,21 @@ int main(int argc, char **argv)
 
     pthread_create(&id, NULL, runner_handler, NULL);
 
-    usleep(1500000);
+    /* Bubble can need more than one second to acquire DRM and initialize GLES.
+     * Wait for the ARMHF producer's first frame without racing the launcher's
+     * shared-memory readiness probe. */
+    for (int startup_wait = 0; startup_wait < 200 && frame_cnt == 0; startup_wait++) {
+        usleep(50000);
+    }
 
     if (frame_cnt == 0) {
         running = 0;
         error("no receive any frame from emulator\n");
 
 #if defined(TRIMUI_BRICK)
         system("kill -9 `pidof ld-linux-armhf.so.3`");
 #endif
 
-#if defined(GKD_PIXEL2) || defined(GKD_MINIPLUS)
+#if defined(GKD_PIXEL2)
         system("kill -9 `pidof drastic`");
 #endif
diff --git a/sdl2/src/video/nds/nds_event.h b/sdl2/src/video/nds/nds_event.h
index 6e2d240..46cc8ad 100644
--- a/sdl2/src/video/nds/nds_event.h
+++ b/sdl2/src/video/nds/nds_event.h
@@ -240,14 +240,15 @@
 #define DEV_KEY_CODE_R2         313
 #define DEV_KEY_CODE_START      315
 #define DEV_KEY_CODE_SELECT     314
 #define DEV_KEY_CODE_MENU       316
+#define DEV_KEY_CODE_DRASTIC    704
 #define DEV_KEY_CODE_POWER      -1
 #define DEV_KEY_CODE_VOL_UP     -1
 #define DEV_KEY_CODE_VOL_DOWN   -1
 
 #define DEV_KEY_CODE_SAVE       318
 #define DEV_KEY_CODE_LOAD       317
-#define DEV_KEY_CODE_FAST       704
+#define DEV_KEY_CODE_FAST       -1
 #define DEV_KEY_CODE_EXIT       -1
 #endif
 
diff --git a/sdl2/src/video/nds/nds_event.c b/sdl2/src/video/nds/nds_event.c
index 7f9dc07..47c39a7 100644
--- a/sdl2/src/video/nds/nds_event.c
+++ b/sdl2/src/video/nds/nds_event.c
@@ -1063,6 +1063,12 @@ static int update_key_bit(uint32_t c, uint32_t v)
         trace("set KEY_BIT_MENU\n");
         set_key_bit(KEY_BIT_MENU, v);
     }
+#if defined(GKD_MINIPLUS)
+    if (c == DEV_KEY_CODE_DRASTIC) {
+        trace("set KEY_BIT_DRASTIC\n");
+        set_key_bit(KEY_BIT_DRASTIC, v);
+    }
+#endif
     if (c == myevent.keypad.save) {
         trace("set KEY_BIT_SAVE\n");
         set_key_bit(KEY_BIT_SAVE, v);
 
